import type { Step } from '@/types'
import React from 'react'

const ProgressStep = ({currentStep,steps}:{currentStep:number, steps: Step[]}) => {
  return (
    <div>ProgressStep</div>
  )
}

export default ProgressStep